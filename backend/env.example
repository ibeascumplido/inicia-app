# MongoDB Atlas (obligatorio)
MONGO_URL=mongodb+srv://usuario:password@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=menu_facturacion_inicia

# Orígenes CORS (opcional en Vercel: se auto-detecta VERCEL_URL)
# CORS_ORIGINS=https://tu-app.vercel.app,http://localhost:3000

# Email con Resend (opcional; sin clave válida no se envían correos)
RESEND_API_KEY=
SENDER_EMAIL=onboarding@resend.dev
