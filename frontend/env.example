# Desarrollo local: URL del backend
REACT_APP_BACKEND_URL=http://localhost:8000

# Vercel (mismo dominio): dejar vacío o no definir esta variable.
# Las peticiones irán a /api en el mismo dominio del frontend.
